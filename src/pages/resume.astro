---
import Base from "../layouts/Base.astro";
import { site } from "../data/site";
import { experience } from "../data/experience";
import { skills } from "../data/skills";
import { education, achievements, certifications, languages } from "../data/about";
import { projects } from "../data/projects";

const summary =
  "Software engineer building mobile and web products in Flutter, currently at Vietnam Silicon. I run AI coding agents in production to lift delivery velocity while keeping code quality through rigorous review. B.Eng in Software Engineering, UIT (VNU-HCM).";
---

<Base title={`${site.name} — Résumé`}>
  <main class="resume">
    <header class="r-head">
      <h1>{site.name}</h1>
      <p class="r-role">{site.role} · Mobile Developer</p>
      <p class="r-contact">
        <a href={`mailto:${site.email}`}>{site.email}</a>
        <span>·</span>
        <a href={site.links.github}>github.com/AKaLee-IK27</a>
        <span>·</span>
        <a href={site.links.linkedin}>linkedin.com/in/akl0101</a>
        <span>·</span>
        <span>{site.location}</span>
      </p>
    </header>

    <section>
      <h2>Summary</h2>
      <p class="r-summary">{summary}</p>
    </section>

    <section>
      <h2>Experience</h2>
      {
        experience.map((role) => (
          <div class="r-role-block">
            <div class="r-role-line">
              <span class="r-company">{role.company}</span>
              <span class="r-period">{role.period}</span>
            </div>
            <p class="r-title">
              {role.title} · {role.location}
            </p>
            <ul>
              {role.highlights.map((h) => (
                <li>{h}</li>
              ))}
            </ul>
            <p class="r-tags">Tech: {role.tags.join(" · ")}</p>
          </div>
        ))
      }
    </section>

    <div class="r-grid">
      <section>
        <h2>Projects</h2>
        <ul class="r-projects">
          {projects.slice(0, 3).map((p) => (
            <li>
              <strong>{p.name}</strong> — {p.blurb}
              <span class="r-proj-tags">({p.tags.join(", ")})</span>
            </li>
          ))}
        </ul>
      </section>

      <section>
        <h2>Skills</h2>
        <ul class="r-skills">
          {
            skills.map((g) => (
              <li>
                <strong>{g.label}:</strong> {g.items.join(", ")}
              </li>
            ))
          }
        </ul>
      </section>

      <section>
        <h2>Education</h2>
        {
          education.map((e) => (
            <p class="r-edu">
              <strong>{e.school}</strong>
              <br />
              <span>{e.detail} · {e.period}</span>
            </p>
          ))
        }
        <h2 class="r-mt">Recognition</h2>
        <ul class="r-mini">
          {achievements.map((a) => <li>{a.title}{a.detail ? `, ${a.detail}` : ""} ({a.year})</li>)}
          {certifications.map((c) => <li>{c.title} ({c.year})</li>)}
        </ul>
        <h2 class="r-mt">Languages</h2>
        <p class="r-langs">{languages.map((l) => `${l.name} (${l.level})`).join(" · ")}</p>
      </section>
    </div>
  </main>

  <style is:global>
    @page {
      size: A4;
      margin: 16mm 16mm;
    }
    body {
      background: #ffffff;
    }
    .resume {
      max-width: 800px;
      margin: 0 auto;
      padding: 32px 28px 48px;
      color: #16241b;
      font-size: 10.6pt;
      line-height: 1.5;
    }
    .resume a {
      color: #2c5f2d;
      text-decoration: none;
    }
    .r-head {
      border-bottom: 2px solid #4e9f3d;
      padding-bottom: 12px;
      margin-bottom: 18px;
    }
    .r-head h1 {
      font-size: 26pt;
      font-weight: 600;
      letter-spacing: -0.01em;
      margin: 0;
      font-family: var(--font-display);
    }
    .r-role {
      color: #2c5f2d;
      font-weight: 600;
      margin: 2px 0 6px;
    }
    .r-contact {
      color: #5d6b58;
      font-size: 9.5pt;
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }
    .resume h2 {
      font-size: 12pt;
      letter-spacing: 0.04em;
      color: #2c5f2d;
      font-weight: 700;
      margin: 14px 0 8px;
      font-family: var(--font-display);
    }
    .r-summary {
      margin: 0;
    }
    .r-role-block {
      margin-bottom: 8px;
      break-inside: avoid;
    }
    .r-role-line {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 12px;
    }
    .r-company {
      font-weight: 600;
      font-size: 11.5pt;
    }
    .r-period {
      color: #5d6b58;
      font-size: 9.5pt;
      white-space: nowrap;
    }
    .r-title {
      color: #2c5f2d;
      font-weight: 500;
      margin: 1px 0 4px;
      font-size: 10pt;
    }
    .resume ul {
      margin: 0;
      padding-left: 16px;
    }
    .resume li {
      margin-bottom: 2px;
    }
    .r-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-top: 14px;
    }
    .r-skills li {
      margin-bottom: 4px;
      list-style: none;
    }
    .r-skills {
      padding-left: 0;
    }
    .r-edu {
      margin: 0 0 8px;
    }
    .r-edu span {
      color: #5d6b58;
      font-size: 9.5pt;
    }
    .r-mt {
      margin-top: 14px;
    }
    .r-mini {
      font-size: 9.8pt;
    }
    .r-langs {
      margin: 0;
      font-size: 9.8pt;
    }
    .r-tags {
      color: #5d6b58;
      font-size: 9pt;
      margin: 2px 0 0;
      padding-left: 16px;
    }
    .r-projects {
      padding-left: 16px;
      font-size: 9.8pt;
    }
    .r-projects li {
      margin-bottom: 4px;
    }
    .r-proj-tags {
      color: #5d6b58;
      font-size: 9pt;
    }
    /* keep the document calm in print; the brand color stays in accents only */
    @media print {
      .resume {
        padding: 0;
        max-width: none;
      }
    }
  </style>
</Base>
